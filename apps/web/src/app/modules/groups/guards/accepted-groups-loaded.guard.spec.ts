// import { TestBed, async, inject } from '@angular/core/testing';

// import { AcceptedGroupsLoadedGuard } from './accepted-groups-loaded.guard';

// describe('AcceptedGroupsLoadedGuard', () => {
//   beforeEach(() => {
//     TestBed.configureTestingModule({
//       providers: [AcceptedGroupsLoadedGuard]
//     });
//   });

//   it('should ...', inject([AcceptedGroupsLoadedGuard], (guard: AcceptedGroupsLoadedGuard) => {
//     expect(guard).toBeTruthy();
//   }));
// });
